# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Planning artifacts/docs
BUGS.md
SCOPING.md
HOMELAB_SETUP.md
WIRING.md

# Build artifacts (cmake, ESP-IDF, etc.)
build/
.cache/

# ESP-IDF generated (keep sdkconfig.defaults; the rest is per-machine / fetched)
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# macOS
.DS_Store
# AppleDouble sidecar files (resource-fork/xattr metadata on non-native volumes)
._*
