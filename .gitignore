# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Build artifacts (cmake, ESP-IDF, etc.)
build
.cache

# Test data generated via Microsoft's edge-tts (src/tests/generate_golden.py)
src/tests/golden/*.wav

# KiCad per-user/local state and backups -- not design data
*.kicad_prl
*.bak
~*.lck
.history/

# ESP-IDF generated (keep sdkconfig.defaults; the rest is per-machine / fetched)
sdkconfig
sdkconfig.old
managed_components
dependencies.lock

# macOS
.DS_Store

# AppleDouble
._*
