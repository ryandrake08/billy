# Billy fish partition table — budgeted against a 4 MB flash target (see sdkconfig.defaults),
# even though the current bench board (ESP32-S3-WROOM-1 N8R8) physically has 8 MB.
# Single factory app, sized with headroom for the audio/transport/motor code (app is ~1.3 MB
# today). No OTA -- updates are reflashed over USB, so no second app slot is needed.
# Offsets/sizes in bytes.
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
phy_init,   data, phy,     0xf000,   0x1000
factory,    app,  factory, 0x10000,  0x300000
